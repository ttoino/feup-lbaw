export interface Datetime {
    iso: string;
    long_diff: string;
    diff: string;
    datetime: string;
    date: string;
    time: string;
}

export interface Markdown {
    raw: string;
    formatted: string;
}
